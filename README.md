# لوحة تحكم الطلبات - كافيه فيروز

## نظرة عامة
لوحة تحكم تفاعلية لإدارة طلبات كافيه فيروز، تعرض الطلبات الجديدة في الوقت الفعلي مع إمكانية تحديث حالاتها مباشرة من الواجهة.

## رابط الموقع المستضاف
يمكن الوصول إلى اللوحة من خلال الرابط التالي:  
**[لوحة تحكم الطلبات](https://ahmed-hazem-1.github.io/mogeeb-dashboard/)**

## المميزات

### 🔄 التحديث التلقائي
- تحديث تلقائي كل 30 ثانية
- إمكانية تفعيل/إيقاف التحديث التلقائي
- عرض وقت آخر تحديث

### 📊 عرض البيانات
- عرض تفاصيل كاملة لكل طلب (العميل، الأصناف، السعر)
- إحصائيات سريعة (العدد الكلي، المؤكدة، قيد التحضير)
- ألوان مميزة لكل حالة طلب

### 🔔 التنبيهات
- تنبيهات صوتية للطلبات الجديدة
- إشعارات المتصفح
- تأثيرات بصرية للطلبات الجديدة

### ⚡ التفاعل
- تحديث حالة الطلبات بنقرة واحدة
- نوافذ تأكيد قبل التحديث
- رسائل نجاح وخطأ

### ⚙️ الإعدادات القابلة للتخصيص
- روابط الويب هوك قابلة للتعديل
- حفظ الإعدادات في localStorage
- واجهة مستخدم لتعديل الإعدادات

## الملفات

### `index.html`
الملف الرئيسي للواجهة يحتوي على:
- هيكل HTML للوحة التحكم
- أقسام للإحصائيات وعرض الطلبات
- نوافذ التأكيد والرسائل

### `config.html`
صفحة الإعدادات:
- تعديل روابط الويب هوك
- تعديل فترة التحديث التلقائي
- حفظ الإعدادات محلياً

### `styles.css`
ملف التنسيق يشمل:
- تصميم عصري ومتجاوب
- ألوان مميزة لكل حالة
- تأثيرات انتقالية ناعمة
- دعم الطباعة والأجهزة المحمولة

### `script.js`
منطق التطبيق الرئيسي:
- التفاعل مع الـ webhooks
- إدارة حالة التطبيق
- معالجة التنبيهات والإشعارات
- تحديث الواجهة ديناميكياً

## نشر التطبيق

### النشر على GitHub Pages

1. قم بإنشاء مستودع جديد على GitHub باسم `mogeeb-dashboard`
2. قم برفع الملفات إلى المستودع:
```bash
git init
git add .
git commit -m "نسخة أولية من لوحة التحكم"
git branch -M main
git remote add origin https://github.com/ahmed-hazem-1/mogeeb-dashboard.git
git push -u origin main
```

3. قم بتفعيل GitHub Pages:
   - انتقل إلى إعدادات المستودع
   - اختر قسم "Pages"
   - اختر فرع "main" والمجلد "/ (root)"
   - انقر على "Save"

4. بعد النشر، قم بتكوين روابط الويب هوك:
   - انتقل إلى صفحة `config.html` في الموقع المنشور
   - أدخل روابط الويب هوك الخاصة بك
   - انقر على "حفظ الإعدادات"

## إعداد n8n

تأكد من إعداد n8n وفقاً للتعليمات الموجودة في ملف `n8n-setup.md`

### 1. إعداد n8n Workflows

#### Workflow للجلب (GET)
- **المشغل**: Webhook
- **النوع**: GET
- **الرابط**: المتوفر حالياً
```
 
```

#### Workflow للتحديث (POST) - يحتاج إنشاء
- **المشغل**: Webhook  
- **النوع**: POST
- **البيانات المطلوبة**:
```json
{
  "order_id": 28,
  "new_status": "completed"
}
```

- **استعلام التحديث**:
```sql
UPDATE orders 
SET status = '{{ $json.body.new_status }}' 
WHERE order_id = {{ $json.body.order_id }};
```

### 2. تحديث الإعدادات
في ملف `script.js`، قم بتحديث:
```javascript
const CONFIG = {
    // رابط جلب الطلبات (متوفر)
    GET_ORDERS_WEBHOOK: 'رابط_الـ_webhook_للجلب',
    
    // رابط تحديث الطلبات (يحتاج إنشاء)
    UPDATE_ORDER_WEBHOOK: 'رابط_الـ_webhook_للتحديث'
};
```

### 3. تشغيل اللوحة
1. افتح `index.html` في المتصفح
2. اسمح بالإشعارات عند الطلب
3. ستبدأ اللوحة بتحميل البيانات تلقائياً

## طريقة الاستخدام

### عرض الطلبات
- تظهر الطلبات النشطة فقط (confirmed, preparing)
- كل طلب يعرض في بطاقة منفصلة
- الألوان تدل على حالة الطلب:
  - 🟢 أخضر: مؤكد (confirmed)
  - 🟡 أصفر: قيد التحضير (preparing)

### تحديث الطلبات
1. انقر على الزر المناسب في بطاقة الطلب
2. اختر الإجراء:
   - **بدء التحضير**: لتغيير من "مؤكد" إلى "قيد التحضير"
   - **الطلب جاهز**: لإتمام الطلب
3. أكد الإجراء في النافذة المنبثقة

### التحكم في التحديث
- **زر التحديث**: تحديث فوري للبيانات
- **تبديل التحديث التلقائي**: تفعيل/إيقاف التحديث كل 30 ثانية

## حالات العرض

### 🔄 جاري التحميل
تظهر أثناء جلب البيانات من الخادم

### ❌ خطأ الاتصال
تظهر عند فشل الاتصال مع إمكانية إعادة المحاولة

### 🎉 لا توجد طلبات
تظهر عندما تكون جميع الطلبات مكتملة

### 📱 التصميم المتجاوب
- يتكيف مع أحجام الشاشات المختلفة
- تصميم محسّن للهواتف والأجهزة اللوحية

## التخصيص

### تغيير فترة التحديث
```javascript
// في script.js
const CONFIG = {
    AUTO_REFRESH_INTERVAL: 30000, // 30 ثانية
    // غيّر القيمة حسب الحاجة
};
```

### تخصيص الألوان
```css
/* في styles.css */
.order-card.confirmed {
    border-right-color: #27ae60; /* أخضر للمؤكدة */
}

.order-card.preparing {
    border-right-color: #f39c12; /* أصفر للتحضير */
}
```

### إضافة حالات جديدة
1. أضف الحالة في `getStatusText()`
2. أضف التنسيق في CSS
3. أضف الأزرار في `createActionButtons()`

## استكشاف الأخطاء

### مشاكل الاتصال
- تأكد من عمل ngrok
- تحقق من روابط الـ webhooks
- راجع وحدة تحكم المتصفح للأخطاء

### مشاكل البيانات
- تأكد من تنسيق JSON الصحيح
- راجع استعلامات قاعدة البيانات
- تحقق من أسماء الحقول

### مشاكل الواجهة
- امحح ذاكرة التخزين المؤقت للمتصفح
- تأكد من تحديث المتصفح
- تحقق من دعم JavaScript

## الأمان

### تأمين الـ webhooks
- استخدم HTTPS دائماً
- أضف authentication headers
- قم بتشفير البيانات الحساسة

### حماية البيانات
- لا تعرض معلومات العملاء الحساسة
- استخدم CORS headers المناسبة
- قم بتسجيل العمليات المهمة

## التطوير المستقبلي

### مميزات مقترحة
- [ ] إضافة فلترة الطلبات
- [ ] تصدير التقارير
- [ ] إحصائيات متقدمة
- [ ] دعم عدة فروع
- [ ] تكامل مع نظام الدفع
- [ ] تطبيق جوال

### تحسينات الأداء
- [ ] التحميل التدريجي للطلبات
- [ ] ذاكرة تخزين محلية
- [ ] ضغط البيانات
- [ ] تحميل البيانات في الخلفية

---

**ملاحظة**: تأكد من إنشاء الـ webhook للتحديث في n8n وتحديث الرابط في الكود قبل الاستخدام الكامل.